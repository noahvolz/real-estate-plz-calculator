// model.js
// Placeholder for the full financial model. We'll implement it in Step 3.

/**
 * simulateScenario will later perform the full Excel-like calculation.
 * For now it just returns a simple object so we can test wiring.
 */
export function simulateScenario(inputs, socioContext) {
  console.log("simulateScenario called with:", inputs, socioContext);

  return {
    years: [],
    kpis: {
      message: "Model not implemented yet",
    },
  };
}
